# Testing

This document marks the current point where manual app testing can start.

Reader regression result on July 13, 2026: the restored visible-overflow page strip renders the real EPUB on the target device, and all four independent padding controls visibly update the reading surface. Top/Bottom use Android layout padding around the `WebView`, outside the EPUB HTML; Left/Right update the page strip in place.

## Start Here

You can begin manual testing once these conditions are true:

- `.\gradlew.bat assembleDebug` passes
- `.\gradlew.bat testDebugUnitTest` passes
- `.\gradlew.bat assembleDebugAndroidTest` passes if you plan to run instrumentation tests
- a reachable BookOrbit server URL is available
- at least one account can sign in and access a library with real content

Current automated coverage includes the EPUB Readium suite plus `ReadiumComicReaderRoutingTest` for direct CBZ routing, connected CBR/CB7 routing, and image signatures. Two connected `ReadiumComicOpenInstrumentedTest` cases exercise CBZ opening/locator behavior, authenticated-page preparation and cache reuse, and a real injected center tap opening Lagrange's comic controls.

Reader/detail baseline on July 20, 2026: version `0.2.7` (`versionCode` 9) isolates reader screen ownership from delayed browser updates and uses status-bar-safe chrome with no visible Back action, leftmost labeled Exit/X, reading-surface/center-scrim dismissal, a right-side 75%-height page rail, separate EPUB chapter arrows, and outer-only chapter/page selection. The tutorial lasts exactly 3,000 ms; every full-screen Previous/Menu/Next region consumes taps and dismisses immediately. The current full gate passes 258 JVM tests across 44 suites plus `lintDebug`, `assembleDebug`, and Android-test APK assembly. Target-device feedback confirms sync/refresh/download activity no longer replaces the reader, Exit works, tutorial behavior/readability works, Series grouping/persistence works, and profile-menu ordering works; broader format/responsive checks plus the cog icon, divider semantics, and About routing remain pending.

Tutorial-label readability follow-up: the black rounded label backing is removed and Previous/Menu/Next use 28 sp bold white text over the existing colored regions. `ReaderTapZoneTutorialTest` and `compileDebugAndroidTestKotlin` pass; target-device readability validation passes.

Series-detail grouping follow-up: unscoped Series BookCard payloads omit `libraryId`, so cross-library Series books are now loaded through scoped per-library Series requests and ownership is merged into the canonical list by book ID; Library grouping therefore preserves and separates ownership correctly. Section labels share their row with a trailing divider. `SeriesBookGroupingTest` covers configured Library section order, normalized alphabetical File format sections, series-index ordering, the ungrouped state, and mutually exclusive toggle behavior. `AppPreferencesStoreTest` covers NONE/LIBRARY/FORMAT persistence with Library as the default; compiled Compose coverage switches between all three states and the recreation test persists Format. Focused JVM tests and `compileDebugAndroidTestKotlin` pass. Target-device feedback confirms cross-library separation, grouping persistence, and the label/divider layout.

Profile-menu follow-up: compiled `BookOrbitAppInstrumentedTest` coverage asserts Achievements → Options → About ordering, the Options cogwheel semantics, the divider before Change server/Log out, About navigation, and More containing Series/Authors/Local books without About. `compileDebugKotlin` and `compileDebugAndroidTestKotlin` pass; target-device feedback confirms the menu ordering. Physical checks of the cog icon, divider semantics, and About routing remain pending.

Readium/audio/cover-shape work order: EPUB and CBZ are target-device validated as direct Readium publications; connected CBR is target-device validated after authenticated BookOrbit page extraction builds a cached CBZ for Readium. Normal Read and Preview audio return to the browser with a persistent compact-only player above bottom navigation and in separate Readium activities. It exposes tappable cover-to-detail, title/author, elapsed/remaining seek slider, Replay 10, play/pause, Forward 30, BookOrbit `audioMetadata` chapter selection, 0.75/1/1.25/1.5/2× speed, and Close; audiobook detail says Play. Metadata is loaded before preparation and persisted through active-reader/detail caches. Online Preview downloads an authenticated temporary audio copy and never persists progress. `CookieManager` clear is main-thread confined after the connected path exposed it. The full gate passes 258 JVM tests across 44 suites with zero failures/errors/skips, `lintDebug`, `assembleDebug`, and Android-test APK assembly. `BookOrbitRepositoryIntegrationTest#nonLocalAudiobookPreviewDownloadsAnAuthenticatedTemporaryReaderCopy` passes on the configured Medium_Phone emulator. The earlier crash/audio opening was user-device confirmed before the July 21 regression; current Play/Preview, notification/background/process recreation, cover-aspect contracts, and the remaining format matrix still need validation/work.

Audiobook perpetual-preparation regression (July 21): `AppCoordinator` previously enriched chapters by replacing the selected catalog/download `BookSummary` with a detail response's independently parsed book identity. The resulting `fileId`, format, or `localPath` mismatch could strand Readium on Preparing. The regression fix preserves the selected identity and copies only `audioChapters`. Cache coverage now uses a new audio-cache version and format suffix, staged writes, complete/nonempty/readable validation, and atomic promotion. `interruptedAudiobookPreviewDoesNotReuseAPartialReaderCopy` passes on Medium_Phone; the real 489,114,453-byte M4B also opens when the compact player observes the session before playback, and the session is delivered. The full gate remains 258 JVM tests/44 suites with zero failures/errors/skips plus `lintDebug`, `assembleDebug`, and `assembleAndroidTest`. Install `app/build/outputs/apk/debug/app-debug.apk` and revalidate Play and Preview on the user device, including retry after interrupting Preview during preparation. Neither path may spin beyond the 30-second preparation timeout or reuse a partial cache entry.

Audiobook crash regression: running the externally pushed 489,114,453-byte M4B on the emulator reproduced Media3 `IllegalStateException: Player is accessed on the wrong thread` because a main-looper Readium ExoPlayer was configured on `DefaultDispatcher-worker-1`. Parsing remains on IO; navigator/ExoPlayer creation, MediaSession opening, play, and close now execute on `Dispatchers.Main.immediate`. `ReadiumAudioOpenInstrumentedTest` verifies the real fixture opens, plays, activates MediaSession/foreground service/audio focus, and closes. Coordinator regressions verify audio is not saved active before successful playback and any preparation exception logs, clears stale reader state, returns to Browser, and cannot restart-loop. The user confirmed that fix before the subsequent preparation regression; repeat it with the July 21 build.

## Latest device feedback — July 12, 2026

- Server setup and login passed.
- Full relaunch with an existing session passed.
- Library browsing and navigation passed.
- EPUB reading and the current pagination behavior passed.
- Download, airplane-mode reopen, and progress-sync behavior passed.
- The previous launch visual issue was a spinning loading indicator instead of the expected app-specific adaptive-icon presentation; it is now replaced in code with a branded splash/loading state and needs physical-device confirmation.
- The next device pass must validate the new first-row Currently reading shelf and the implemented Plex-inspired shell: Home/Libraries/More bottom navigation, More expansion, Home-only logo/search/profile actions, the tappable Library name selector, Recommended/Browse tabs, visible status bar/Home spacing, and search layer. It must also validate represented-label #/letter jump rails, series ordering after collapse/expand, and the branded launch state. Session-expiry recovery is already device-validated.
- Exact in-chapter EPUB restore, compact poster-card library browsing, Lagrange branding with the subtitle `a BookOrbit reader` on splash/loading only, the Libraries series-collapse control, Local books before Options in More, the placeholder About destination, Home/Library swipe-down refresh, and persistent cover-thumbnail caching are implemented. Physical-device validation remains required for the new behavior.
- The latest reader follow-up restores the same single visible-overflow strip used before the blank-screen regression. Target-device testing confirms that EPUB content renders and that Top/Bottom WebView resizing plus Left/Right in-page updates visibly change the reader padding.
- The July 13 follow-up adds refresh-cookie retry before session-expired recovery, retried Series catalog covers with in-memory image caching, immediate Continue reading updates, smaller shared typography, and title/series/index card metadata rows. The Series thumbnail follow-up corrects its URL contract to use BookOrbit's `coverBookIds` and `/books/{bookId}/thumbnail` rather than a nonexistent Series cover route. These changes require physical-device validation.
- The progress reconciliation follow-up parses BookOrbit's numeric `readingProgress` and nested `readStatus`, keeps all reader and API values on a single 0-100 scale, permits newer reread/backward corrections, and explicitly writes `reading`/`read` status as part of the queued progress operation. Bidirectional Android/BookOrbit synchronization is validated on the target server.
- The catalog follow-up replaces Browse lazy loading with a complete server-scoped Room metadata cache. Cached content renders before cold-start network checks finish; refresh reconciles all pages and writes only changed/new/reordered rows or deletions. Exact default-sort rail taps use BookOrbit jump-bucket indexes. JVM tests and instrumentation-test compilation pass; target-device/server validation is required.
- The large-library follow-up cancels off-screen cover calls, warms versioned selected-library thumbnails in unmetered WorkManager batches, and caches rich details per catalog version. The full pass completes 127 JVM tests across 21 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation; target-device validation with the 5k-book library is required.
- The July 15 Currently Reading/On Deck pass and normal-user reset follow-up complete 138 JVM tests across 21 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. The follow-up replaces the metadata-admin reset endpoint with BookOrbit's normal-user primary-file/audio progress and unread-status APIs; target-device validation must confirm this fallback with an account that lacks metadata-edit permission, plus the overflow/long-press interaction, restart behavior, and revised On Deck sequence.
- The July 15 Home refresh and Series thumbnail pass completes 139 JVM tests across 21 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Target-device validation must confirm the Home pull gesture and representative Series thumbnails against the live server.
- The July 15 complete Series navigation pass completes 142 JVM tests across 21 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Target-device/server validation must confirm multi-page loading, the absence of Load more, and exact ascending/descending rail navigation on the full Series catalog.
- The July 15 collapsed Libraries series-count pass completes 143 JVM tests across 21 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. It adds JVM grammar/grouping coverage and a compiled Compose assertion for the visible count. Target-device validation must confirm the count remains readable on narrow cards and matches the books represented by the active Browse filter.
- The July 15 EPUB theme-persistence pass completes 144 JVM tests across 22 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. It adds round-trip/fallback coverage plus a compiled Android SharedPreferences recreation test. Target-device validation must confirm the selected app-wide theme survives reader close, another EPUB, and a full app relaunch.
- The July 15 reader keep-awake pass completes 145 JVM tests across 23 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. It adds visual-format selection coverage and a compiled Compose lifecycle test that clears the keep-awake flag on exit. Target-device validation must confirm an idle visual reader outlives the configured display timeout while explicit system locking still works.
- The July 15 reader-options pass retains 145 JVM tests across 23 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Its compiled Compose interaction test verifies there is no visible Back action, exactly one Close action, and both Close and exposed-content dismissal paths. Target-device validation must confirm Android Back dismisses options before exiting the reader and exposed-content taps do not turn a page.
- The July 15 native reader-status pass completes 146 JVM tests across 24 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Policy coverage verifies a visible top status bar, immersive bottom navigation, and theme-appropriate icon contrast. Target-device validation must confirm Android's battery and network indicators remain legible across Light, Sepia, and Dark.
- The July 15 EPUB progress-footer pass completed 148 JVM tests across 25 suites with zero failures, covering the earlier footer contract; the later pagination step replaces its normalized location with measured pages. Target-device validation must confirm the measured footer stays readable, theme-matched, and unobstructed on representative screen sizes.
- The July 15 unread-refresh follow-up completes 149 JVM tests across 25 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Its regression fixture includes zero progress plus stale page, position, book, progress, and unread-status timestamps and verifies that none can reconstruct Currently Reading activity. Target-device confirmation remains required against the live server response.
- The July 15 large-catalog refresh pass completes 150 JVM tests across 25 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Its coroutine regression verifies 100-book page ordering, a four-request concurrency ceiling, and complete 1,000-book merging. Target-device timing and server-load confirmation remain required with the roughly 5,000-book library.
- The July 15 HTTP-server pass retains 150 JVM tests across 25 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. URL coverage verifies explicit remote HTTP, HTTPS and bare-host defaults, unsupported-scheme rejection, and a compiled Compose setup flow that submits an explicit HTTP URL. Target-device confirmation remains required against the intended HTTP BookOrbit deployment.
- The July 15 book-status action pass completes 153 JVM tests across 25 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. Coordinator and payload coverage verifies position-preserving Mark as read and progress-resetting Mark as unread; Room coverage verifies immediate cached read status, and compiled Compose coverage exercises overflow plus long-press menus. Target-device/server confirmation is deferred with the rest of the manual pass.
- The July 15 reader-options bottom-sheet pass completes 154 JVM tests across 26 suites with zero failures, Android lint, debug APK assembly, and instrumentation-test compilation. It replaces the split top/bottom overlay with one rounded bottom sheet, separates Continue reading from Close book, groups reading-position/appearance/page-margin controls, verifies normal-text contrast in Light, Sepia, and Dark, and compiles interaction coverage for both actions plus exposed-content dismissal. Target-device presentation and interaction validation remains required.
- The book-detail UI pass adds compact horizontally swipeable labeled action tiles with selective icons, tappable Series navigation, separately wrapped Genre and Tag chips, grouped metadata cards, and a full-screen cover viewer. Its Compose regression covers the action area, metadata content, tap-to-dismiss cover viewer, and Series navigation. `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass; a connected device or emulator was not used for this automated pass.
- Follow-up feedback is implemented: detail actions retain compact spacing while showing Read/Preview/Download/Delete local labels beside clear icons; titles expand to five rows, series/index rows remain visible, multi-book selection supports bulk read/unread, and the cover viewer dismisses from any screen tap. Genre chips navigate to paginated filtered Books/Series results; tags remain informational. Native username/password remains the only authentication flow; direct OIDC/SSO is deferred. Audiobook work can now use the local M4B/chapter-metadata fixture. The manga unsupported-format regression is fixed in code: bare CBZ/CBR/CB7 formats route to authenticated remote comic pages, while ZIP-based local comics retain offline extraction.
- The audited follow-up completes 156 JVM tests across 26 suites with zero failures, errors, or skips. Read/Preview labels and clear icons, Download progress/retry/cancel states, multi-select pruning/header layout, and paginated genre navigation are covered by the implementation. `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass; the debug APK is at `app/build/outputs/apk/debug/app-debug.apk`. Genre filtering was later target-server validated.
- The completed book-detail step passes `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleDebugAndroidTest`. Download/Delete local labels, catalog-version/local-path/download-active detail invalidation, stale rich-detail cache removal, and non-clickable Tag regression coverage are included.
- The completed Home work-order step passes `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleDebugAndroidTest`: 158 JVM tests across 26 suites, with zero failures, errors, or skips. Coordinator message clearing, close-button/swipe dismissal, and Recently read filtering regressions are covered.
- The completed EPUB whole-book pagination step passes the full command with 161 JVM tests across 26 suites, zero failures, errors, or skips. Unit coverage verifies chapter-page aggregation, unequal chapter weighting, final-page behavior, calculating fallback, and measurement JavaScript; instrumentation covers the footer and measurement bridge. APK: `app/build/outputs/apk/debug/app-debug.apk`.
- The completed Options > Interface step covers persisted lock orientation, the flat Follow system/Light/Charcoal/Warm black/OLED black app-theme list, fresh-start destination selection, and Reduce motion immediate jumps. Follow system maps Android light/dark to Light/Charcoal, explicit selections remain stable, and stored legacy `dark` migrates to Charcoal. Target-device feedback validates orientation locking, default opening screen, Reduce motion, and all five themes. The former haptic preference and explicit app-haptic paths are removed.
- The completed Options > Data step passes 170 JVM tests across 28 suites with zero failures/errors/skips: `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleDebugAndroidTest`. Coverage includes cellular download Always/Never/Ask browser behavior, storage sizes and safe cache clearing, current cover-worker network policy reconfiguration, and default-on delete confirmation. Target-device feedback validates cellular behavior, storage/cache clearing, delete-local confirmation, and background network policy. Fresh APK: `app/build/outputs/apk/debug/app-debug.apk`.
- Target-device follow-up confirms Read/Preview labels, live download progress, long-title expansion, series-index presentation, multi-selection, and correct layout-derived EPUB whole-book progress. Download/Delete local labels, immediate open-detail reconciliation after download completion, removal of the misleading Tag tap affordance, dismissible Home messages, and completed-only Recently read are complete.
- The completed browser/local-state step passes 171 JVM tests across 28 suites with zero failures/errors/skips, `lintDebug`, and `assembleDebugAndroidTest`. Coverage verifies immediate local-path updates after download/deletion, Room catalog and legacy snapshot persistence, latest-detail metadata fallback for Local books, Local books revision refresh, and Options navigation dismissing retained book detail. Target-device validation now passes for Delete local changing the open detail back to Download, offline Local books thumbnails, and opening Options from book details.
- The completed Home aggregation/Currently Reading step passes 173 JVM tests across 28 suites with zero failures/errors/skips, `lintDebug`, and `assembleDebugAndroidTest`. Coverage verifies server-wide cached Home restoration with legacy fallback/current downloads, selected-first incremental library-slice replacement, preserved selected-library `books`, partial nonselected-library failure handling, and progress/read/unread/local-path updates across both collections. Target-device/server aggregation and cross-library Currently Reading validation pass; failure-message and latency edge checks remain open.
- The completed fullscreen comic-reader step passes 178 JVM tests across 28 suites with zero failures/errors/skips; `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Unit coverage verifies bare CBZ/CBR/CB7 inference, CB7 filters/extensions, authenticated page URL construction, page-count parsing, ZIP/RAR4/RAR5/7z signatures, and the offline/server-required boundary. Compose instrumentation compiles tap-next, right/left swipes, options opening, Continue reading dismissal, and remote CBR page navigation. Target-device feedback confirms fullscreen interactions plus online/local CBZ and CBR work correctly. APK: `app/build/outputs/apk/debug/app-debug.apk`; CB7 validation remains open and offline RAR/7z extraction remains optional.
- The earlier haptic-perceptibility implementation was superseded and removed by user direction; the preference field/storage, Options row, custom provider, manual requests, and obsolete tests no longer exist.
- The completed book-detail reading-status overflow step passes 183 JVM tests across 30 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Helper unit coverage verifies live status selection and exactly-one-action labeling, while instrumentation compiles the Book Detail overflow flow. Physical-device/server validation remains pending.
- The completed immediate reader-close step passes 184 JVM tests across 30 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Focused coordinator regression coverage verifies that cached Browser state appears before background repository work, captured progress is merged into both selected-library and server-wide collections, non-preview cleanup occurs afterward, Preview remains isolated, and no-cache close uses Loading. Prompt close is target-device validated.
- The completed missing-cover fallback step passes 187 JVM tests across 30 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Three helper tests verify canonical fallback when cover metadata is missing, explicit-then-canonical candidate order, and deduplication when normalization already produces the canonical URL. Physical-device/server validation remains pending.
- The completed EPUB current-chapter slider step passes 188 JVM tests across 30 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Unit coverage verifies the clamped `goToPage` JavaScript command; instrumentation compiles the retained Choose chapter/chip selector plus slider semantics and a WebView runtime regression that confirms the jump and rendered text. Target-device validation passes.
- The completed repository HTTP integration-test step retains 188 JVM tests across 30 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. `BookOrbitRepositoryIntegrationTest` uses MockWebServer 4.12.0 with the real Android context, `BookOrbitRepository`, and stores. Its three compiled cases cover: login POST credential payload, token persistence, and Bearer `/api/v1/auth/me`; actual GET libraries plus paginated POST books request/parsing with page size 100, EPUB, and progress; and an offline queue that produces zero HTTP requests until explicit replay posts file progress plus `reading` status and acknowledges the event. Each case isolates/clears progress stores, cancels unique work, and clears server/session state. The tests were not executed because adb enumeration hung and no usable connected target was available; connected-device execution remains pending.
- The completed Home initial-sync optimization passes 189 JVM tests across 30 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. A gated coordinator regression uses six nonselected libraries to prove the first three start, the next three wait, maximum outer concurrency is three, all successful slices merge, and final `isRefreshing` clears. Physical-device/server latency and load validation remain pending.
- The completed downloaded-book update step passes 196 JVM tests across 31 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Store/model unit coverage verifies source-version persistence, update eligibility including one legacy baseline update, ebook compatibility, and atomic replacement. Compiled Compose and real-repository MockWebServer regressions cover online Update local actions plus preservation/retry after a malformed replacement. Target-device feedback confirms the Update local flow works correctly.
- The corrected BookOrbit genre-contract step passes 197 JVM tests across 31 suites with zero failures; `assembleDebugAndroidTest` also passes. The app now sends the official current relation-filter shape: singular `genre` (and adjacent `author`), operator `includesAny`, and an array value instead of the previous `genres`/`contains`/scalar genre request. A compiled real-repository MockWebServer regression asserts the exact genre payload and parses the filtered result. Target-device/server genre filtering is validated.
- The completed jump-rail layout step passes 198 JVM tests across 31 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Unit coverage verifies 16 dp trailing padding without a rail and 32 dp with a rail. Compiled Compose regressions compare Library and Series card right bounds with the rail's left bound and require the cards to remain clear of it. Target-device spacing is validated; additional responsive-layout validation remains pending.
- The completed Change server step passes 200 JVM tests across 31 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Two coordinator tests verify reachable replacement and unreachable recovery with the attempted URL prefilled. A compiled Compose regression verifies Change server immediately precedes Log out, the current URL prefill, warning text, Cancel returning to the populated editor, and Continue invoking reset/set behavior. Target-device interaction and session-transition validation pass.
- The completed series Previous/Next navigation step passes 203 JVM tests across 32 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Three `SeriesNavigationTest` cases cover unordered numeric-index sorting, stable disabled edges, and null-index ordering with unrelated-book filtering. The transparent borderless controls and basic navigation are target-device validated; additional responsive, long-title, loading, and offline edge states remain open.
- The completed five-option app-theme step passes 205 JVM tests across 33 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Two `AppThemePaletteTest` cases verify Follow-system/manual stability and exact, distinct palette backgrounds/surfaces. Preference unit coverage round-trips every mode and migrates legacy `dark` to Charcoal; instrumentation persists Warm black, and the compiled Options UI regression selects OLED black. All five themes, detailed contrast, and system bars work on the target device.
- The completed Achievements step passes 208 JVM tests across 34 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. The authenticated endpoint works on the target server/device. The compact-card follow-up replaces the oversized posters with 260 dp-minimum information cards and 22 dp server-driven icons; secret/unsupported/retry/responsive edge checks remain pending. APK: `app/build/outputs/apk/debug/app-debug.apk`.
- The completed combined feedback batch passes 206 JVM tests across 33 suites with zero failures/errors/skips after removing the obsolete haptic suite; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. Change server treats the normalized current URL as a silent no-op; Library/Series rails expose only represented buckets without forward-fallback labels; fresh EPUB settings use 30% Top with 15% Bottom/Left/Right while retaining saved values; series neighbors reuse the first complete load behind transparent borderless 46 dp controls; and all explicit app-haptic behavior has been removed. APK: `app/build/outputs/apk/debug/app-debug.apk`.
- Live server logs diagnosed the stuck queue as repeated authenticated `POST /api/v1/books/files/15663/progress` 404 responses from a stale/deleted EPUB file ID, not a transient network failure. The fix passes 207 JVM tests across 33 suites with zero failures/errors/skips; `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. A parser JVM test covers current-primary-file extraction; a compiled real-repository MockWebServer regression proves stale-to-current file remapping, the remapped progress POST plus status PATCH, and queue count draining to zero. APK: `app/build/outputs/apk/debug/app-debug.apk`.
- EPUB page-measurement timing/stability remains a separate representative-device check. Native username/password remains the current authentication flow.

## Latest device feedback — July 15, 2026

### Validation completed

- July 17 feedback confirms swipeable tiles, Series title navigation, wrapped Genre/Tag metadata, grouped metadata cards, text wrapping, reading options, cache opening, exact jumps, thumbnail warnings, rapid-scroll cancellation, detail loading, and airplane mode.
- Audiobook implementation and testing can use the local M4B/chapter-metadata fixture. General comic reading works correctly on the target device. Comic routing is implemented for online CBZ/CBR/CB7 and offline ZIP/CBZ, including mislabeled ZIP archives; online CBZ/CBR/CB7 and offline downloaded comic formats still need broader validation. Downloaded CBR/CB7 is preserved as valid but requires a server connection for page extraction, and offline CBR/CB7 extraction remains optional. Direct OIDC/SSO remains deferred/open.
- The target-device Update local flow works correctly, including the visible action and completion path.

- Explicit HTTP BookOrbit connections work correctly.
- Mark as read and Mark as unread work correctly.
- Android/BookOrbit progress synchronization works correctly.
- Large-library browsing and the #/A–Z jump rail are fast and accurate.
- Genre filtering, all five app themes, system bars, jump-rail spacing, series Previous/Next, Change server, and the functional Achievements flow work correctly on the target device/server. Achievements needs a more compact card redesign.
- Airplane-mode browsing works. Undownloaded books correctly cannot open full details without a connection.
- Session-expiry recovery works as expected.
- Series navigation and Series thumbnails work correctly.

### Latest target-device feedback — July 18, 2026

- Stale progress queue recovery, Download/Delete local reconciliation, offline Local thumbnails, Options-from-detail navigation, server-wide Home/Currently Reading aggregation, prompt reader close, current-chapter slider, series Previous/Next, Change server, jump-rail spacing, theme/system-bar contrast, background network policy, interrupted/large downloads, and online/local CBZ/CBR reading work correctly.
- The oversized Achievement posters have been replaced by compact adaptive information cards with a small leading server-driven icon, title-row lock state, and metadata/progress/date inside each card.
- The final book-detail row is fixed-height and responsive. When More is required, it stays against the right/trailing edge whenever space permits instead of following Preview/other actions toward the left.
- Remote nonlocal EPUB/PDF Preview prepares an authenticated temporary reader copy. The prior extraction-root-only fix still failed physically. The current candidate mounts the current book at appassets `/`, supports parent-relative/root-relative/SVG-wrapped images, and preserves the scoped archive boundary. Install and physically verify the new APK before proceeding to lower-priority UI work.
- The revised Library and Series rails retain the full stable #/A–Z vocabulary when eligible (Z–A/# descending); unrepresented entries are disabled, unavailable, and non-selectable.
- Server-wide and selected-library Local books shelves now appear at the bottom of their respective Home feeds, with deterministic deduplicated alphabetical previews of up to 12 and correctly scoped See all routes.

### Latest implemented UI follow-up

- The compact Achievement/action-row follow-up passes 208 JVM tests across 34 suites with zero failures/errors/skips, lint, debug APK assembly, and Android-test APK assembly. Focused coverage includes `AchievementIconTest`, `BookDetailReadingStatusActionTest`, and Android-test Kotlin compilation. APK: `app/build/outputs/apk/debug/app-debug.apk`.
- The full disabled-letter jump-rail follow-up passes 209 JVM tests across 34 suites with zero failures/errors/skips, lint, debug APK assembly, and Android-test APK assembly. `catalogJumpRailLabels` unit coverage and compiled Compose assertions verify an unavailable `B` in both Library and Series.
- The Local books shelf follow-up passes 210 JVM tests across 34 suites with zero failures/errors/skips, lint, debug APK assembly, and Android-test APK assembly. Coverage verifies aggregation, library scope, deduplication, alphabetical order, the 12-item limit, global See all, and selected-library exclusion.
- The completed EPUB preparation/resource fix passes 213 JVM tests across 35 suites with zero failures/errors/skips; `testDebugUnitTest`, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest` pass. JVM coverage verifies remote EPUB/PDF cache policy, nested/encoded appassets base URLs, and extracted-root boundary rejection. Compiled MockWebServer coverage verifies the authenticated nonlocal EPUB Preview download endpoint; a compiled real-WebView test resolves `../Images/cover.png` with nonzero `naturalWidth`. Instrumentation was not executed because no device was attached. APK: `app/build/outputs/apk/debug/app-debug.apk`.
- The virtual no-store document candidate also passed automation but failed on the S24. The current bitmap-only SVG normalization candidate retains 215 JVM tests across 35 suites with zero failures/errors/skips, `lintDebug`, `assembleDebug`, and `assembleDebugAndroidTest`; permanent `EpubAssetLoadingInstrumentedTest` passes 1/1 on emulator with decoded/nonzero fitted replacement-image assertions. Samsung physical validation remains open.
- Global book-search result list rows now expose Mark as read and Mark as unread through both long-press and a visible three-dot action.
- Search results intentionally remain list rows rather than poster cards.
- The Compose interaction regression covers both menu entry paths and callback actions. JVM tests, instrumentation-test APK compilation, lint, and debug APK assembly pass; no device was attached to execute the instrumentation test.

- Launch, navigation, Home refresh, On Deck, battery indicators, and EPUB footer progress passed on the target device.
- Home remains interactive while its initial sync/loading indicator is visible. Each library still uses 100-item catalog pages with at most four concurrent page requests; after the selected library becomes current, nonselected libraries now use a separate ordered-batch limit of three concurrent library refreshes. Target-device timing and server-load confirmation remain required.
- Currently Reading removal succeeds in the app and BookOrbit's server view, but the tested build repopulated removed titles after Home pull-to-refresh because BookOrbit's explicit unread record carried a fresh status timestamp. The follow-up parser fix now suppresses non-positive unread activity metadata and requires target-device confirmation.
- The remaining reader sync/progress reconciliation test is deferred for now at the user's request.

## Current Priority Manual Pass - July 19, 2026

Run these after each corresponding implementation step, in order:

0. The refresh/navigation isolation fix is implemented and its focused regressions pass. On device, gate or trigger catalog refresh while opening/using a book and confirm `ReaderLoading`/`Reader` remains active while cached browser data updates. Repeat with asynchronous download state/progress updates. Confirm explicit Exit/Close still navigates to Browser and session expiry/sign-out still navigate to authentication destinations.

1. On the Samsung Galaxy S24, install version 0.2.7 from `app/build/outputs/apk/debug/app-debug.apk`. Do not modify the supplied files in `sample/`.
2. Validate local/online CBZ and connected CBR/CB7 through normal Read and Preview. Confirm the dark reader, 25% edge navigation, center-tap lightweight chrome, page rail/footer, leftmost labeled Exit/X with no visible Back action, surface/scrim dismissal, exact normal locator resume/progress, Preview page-1 isolation, orientation lock, keep-awake, and dark system bars. Reopen connected CBR/CB7 to confirm the cached CBZ is reused. Go offline with a downloaded CBR/CB7 and confirm it remains retained but clearly requires the server; reconnect and confirm it opens. Spot-check EPUB, PDF, and audio. Keep the comic migration marked as awaiting physical validation until this pass succeeds.
3. Validate the completed row at narrow, normal, wide, large-text, portrait, and landscape sizes. It must remain exactly 46 dp high with no wrapping, scrolling, clipping, or off-screen actions. Read/Preview stay labeled. For nonlocal books, verify icon-only Download, Retry download semantics after failure, X/Cancel while active, and the same disabled slot offline/unavailable. Once local, confirm no inline transfer; More always contains Delete local and conditionally Update local or Cancel update. Mark as read/unread remains inline only when its measured label fits and otherwise appears in More. At extreme widths, only Read/Preview share weighted remaining width. Verify update status copy directs cancellation/retry through More.
4. Validate the implemented primary-colored line directly above actions. An opened title with real activity may show `Reading · 0%`; explicit unread-reset 0% must show no line. Partial/complete known values use canonical 0–100 and at most two decimals. Genuine unknown reading/completed values show only `Reading` or `Read`. Confirm the identity area shows only availability/format, BrowserState wins over stale cached detail, and Mark as unread removes the line immediately.
5. Validate every EPUB/comic Read and Preview entry shows the tutorial only after rendered navigator readiness, above all reader UI. Confirm equal full-height Previous/Menu/Next thirds, exact red/black/green RGB values at 50% opacity, timeout dismissal exactly 3,000 ms after first pre-draw, and immediate dismissal when any region consumes a tap. Open chrome and verify its top bar starts below the phone status bar, has no visible Back action, and places labeled Exit/X leftmost. Tap the reading surface/center scrim and confirm chrome dismisses without exiting. Confirm the right-side rail is about 75% of screen height; its slider and primary arrows move exactly one page for EPUB/comics, while EPUB's second arrows move chapters and the outer Chapters button selects chapters. Confirm cog options expose no chapter selector or page slider. On book details where More appears, confirm it is close to the row's right edge at narrow/normal/wide widths. Then validate locator/progress, Preview isolation, orientation, keep-awake, themes, accessibility, resume, and offline behavior.

## Manual Test Matrix

### 1. Server And Login

1. Launch the app on an emulator or device.
2. Enter the BookOrbit server URL.
3. Verify explicit `https://` and `http://` BookOrbit URLs are accepted, while unsupported schemes such as `ftp://` are rejected. Confirm a bare remote hostname defaults to HTTPS and a bare local development target such as `10.0.2.2:3000` defaults to HTTP. Use HTTP only on a trusted network because credentials, tokens, metadata, progress, and content are not encrypted.
4. Verify the native username/password form appears with password visibility control and Change server. Direct OIDC/SSO is currently out of scope and remains a deferred work item.
5. Submit valid credentials and confirm the app reaches the library browser.

### 1a. Session Recovery

1. While signed in, fully close and relaunch the app.
2. Confirm an already-valid session returns to the reader or browser without forcing a fresh login.
3. Sign out from the browser screen and confirm the app returns to login cleanly.
4. Latest manual device result on July 10, 2026: explicit sign-out returned to login with the `Change server` action visible, which matches the intended fix for the cached-browser fallback bug.
5. Latest manual device result on July 10, 2026: after signing back in and fully relaunching the app, the existing session returned to the app without forcing a fresh login.
6. If cached Home appears because the server is unavailable, open the drawer and tap `Sign in`; confirm Login remains visible until authentication succeeds.
7. If the reachable server rejects the saved session, confirm cold start opens Login instead of presenting cached Home as if it were merely offline.
8. If possible on the target server, expire the session server-side and confirm the app routes back through login and resumes the intended action after re-authentication.
9. While signed in, trigger search, cover loading, or a library/detail load after expiring the session and confirm the app shows the same session-expired login recovery instead of silently displaying an empty result.
10. If the server exposes refresh-cookie renewal, leave the app idle past the short access-token lifetime or expire only the access token, then browse, load a cover, open a detail screen, and open a downloaded reader file. Confirm the first request renews the session and completes without showing login. Repeat several times over at least 15 minutes; record the server response if login still appears.
11. If possible, trigger two or more authenticated loads together after expiry and confirm only one refresh is needed and none of the loads routes to Login unnecessarily.

### 1b. Physical Device Install

1. Install the latest debug APK on a physical Android device.
2. Confirm the launcher icon is the app-specific adaptive icon instead of the Android default placeholder.
3. Launch the app from the device home screen and confirm the branded adaptive-icon splash/loading state appears without the old spinning loading screen; verify first-run setup still works.
4. Confirm the launcher/app label is Lagrange and the splash/loading presentation shows the subtitle `a BookOrbit reader`; after the app opens, confirm the top bar and About screen do not show that subtitle.

### 2. Library Browsing

1. Confirm the post-login screen opens on Home with the visible Android status bar, intentional top spacing, Lagrange logo, bottom navigation, and search icon visible.
2. Confirm bottom navigation exposes Home, Libraries, and More without a hamburger drawer; open More and verify Series, Authors, Local books, and About appear with comfortable space above the Android navigation area. Open the profile menu and verify Change server, with a swap icon, appears immediately above Log out/Sign in and Options remains above it. Tap Change server and confirm the Server URL editor is prefilled with the current `BrowserState.serverUrl`. Submit that current URL unchanged and again in an equivalent non-normalized form; both must close silently with no warning or state change. Enter malformed input and confirm it remains in the editor with the existing invalid-URL message. Enter a different valid replacement and confirm the next warning says changing server logs out of the current server and cancels active downloads. Tap Cancel and confirm the populated editor returns unchanged. Submit again and tap Continue: active downloads/transient state must cancel, old server/session/cookies/caches/current-reader state must clear, and cached-login fallback must not restore the old browser. For a reachable replacement, confirm Login opens with the normalized URL prefilled and a Server changed message. Repeat with an unreachable replacement and confirm Server Setup opens with the attempted URL and validation error after old state has cleared. Then open Libraries and verify the first/selected library opens directly, the logo is replaced by the tappable library name with a downward-triangle affordance, and tapping it opens library selection. While the picker is open, tap the top-left Libraries title and confirm it returns to the selected library page.
   Open Achievements from the profile menu and confirm the functional flow loads. Verify the earned/available summary remains above Unlocked then Locked sections and that the adaptive grid uses compact information cards with a 260 dp minimum. Each card must show a 22 dp semantic Android Material equivalent for the server-provided official BookOrbit `iconName` immediately before the title, with lock/unlock state at the title row's end. Verify description, category/rarity, earned date for unlocked entries, and current/threshold progress only when both values are present for locked entries remain inside the card. Confirm secret/censored entries do not expose fields the server withheld, narrow and wide layouts remain readable, HTTP 404 produces a clear older-server unsupported state, and another request failure offers Retry and succeeds after recovery.
   Open Options and verify the Interface group exposes lock orientation, one Theme row, default opening screen (Home/Library/Local books), and Reduce motion, with no haptic setting. Open Theme and confirm one flat list in this exact order: Follow system, Light, Charcoal, Warm black, OLED black. Confirm each applies immediately and persists after relaunch. With Follow system selected, switch Android between light and dark and confirm the app follows with Light and Charcoal; select each explicit mode and confirm Android theme changes no longer alter it. Check Charcoal for neutral near-black surfaces, Warm black for visibly warm surfaces, and OLED black for a true-black background with distinct raised surfaces. Across Home, Library, book details, Options, dialogs, cards, errors, and status/navigation bars, verify readable text/icons/outlines, visible blue/amber accents, explicit dark error containers, and no unintended blue cast or lost elevation. Upgrade once from a build storing legacy Dark and confirm it becomes Charcoal. Confirm the selected opening screen is used on a fresh app start. Confirm ordinary Options interactions, long presses, navigation, book taps, and page turns do not trigger app-requested haptics. Open a book detail, invoke the main Options entry point from that state, and confirm Options replaces the detail instead of being masked by retained detail state.
   Verify Data exposes cellular downloads (Always/Never/Ask), downloaded/disposable-cache sizes, confirmed Clear cache that preserves downloaded books/metadata, background network policy (Any network/Wi-Fi only/Disabled), and default-on confirmation before deleting a local copy. Validate behavior on metered cellular and Wi-Fi devices.
3. Confirm Library has Recommended and Browse tabs. Recommended and Browse remain scoped to the selected library, and Browse shows the compact adaptive poster-card grid matching Series and Authors. At the bottom of Library Recommended, verify Local books contains only downloads from the selected library, is deduplicated and alphabetized, shows at most 12 cards with normal actions/covers, and See all opens a Local books screen titled/scoped to that library. When series are present, tap Collapse series and confirm one representative card remains per series with an accurate `1 book` or `N books` label, then tap Expand series and confirm every book returns.
4. Open More > Local books and confirm valid downloads appear in the same poster-card grid with cached thumbnails, open locally, and can still be deleted from book details. After metadata and thumbnails have been cached, disable network access and confirm both the thumbnail and full detail remain available.
5. In Library > Browse on the first open, confirm `Caching full library...` is temporary and the #/letter rail does not appear until the complete catalog is ready. Once eligible, confirm it shows the full stable #/A–Z order (or Z–A/# descending), retains exact server absolute-index jumps for represented targets, and renders missing labels disabled/grey with descriptions such as `B unavailable`, no click action, and no forward fallback. On the roughly 5,000-book library, time one refresh and confirm the indicator clears materially faster than the previous serialized 50-book-page build. Confirm the header totals match BookOrbit and scrolling to the end never triggers another page load. Pull down from Recommended or Browse and confirm cached cards remain visible under `Updating cached catalog...` until refresh completes.
6. Fully close and reopen Lagrange. Confirm cached Home/Browse content appears before the server refresh finishes. Add, rename, change progress on, and delete test titles in BookOrbit, then refresh Lagrange and confirm each change appears while unchanged cards retain their order. Interrupt one refresh if practical and confirm the prior complete catalog remains usable.
7. Tap Filter in Browse and verify title/author/series matching, unread/in-progress/finished, format, and sort/direction controls update immediately from local metadata without a loading-more row. Confirm the letter rail remains exact for server-default, Title, and Author sorts (including descending order) and is hidden for non-letter sorts such as Date added or Read progress. While the rail is visible, confirm the trailing card column ends before the rail with a small gap at every responsive width/orientation; when the rail is hidden, confirm the grid returns to normal full-width 16 dp edge padding.
8. Return Home and confirm shelves aggregate matching books from every library on the connected server, regardless of which library is selected in Libraries. At the bottom, verify Local books aggregates all downloaded/local titles across the server, deduplicates and alphabetizes them, shows at most 12 normal shelf cards, and See all opens the global Local books destination; More > Local books must remain global too. Switch the selected library and confirm Home remains server-wide while Library Recommended/Browse changes scope. From the top of Home, pull down and confirm the selected library updates first and becomes usable/current before other library work. Confirm nonselected libraries refresh in stable ordered batches with no more than three library reconciliations active, completed batches add successful slices incrementally, and final refresh state clears. If one nonselected library can be made temporarily unavailable, confirm its cached slice remains visible and the app names it in the partial-cache message without discarding other refreshed slices. If practical, cancel refresh or expire authentication mid-batch and confirm work stops/routes to login rather than being reported as an ordinary partial-cache failure. Verify each large library's internal catalog traversal still uses its separate four-page-request ceiling.
9. Confirm Continue reading/Currently reading is the first book shelf and that at least one genuinely in-progress title appears there, including a title from a non-selected library. Active progress must appear even if the server read flag is also set; completed books are excluded, and Recently read is a separate history shelf containing only completed books. Swipe a red Home/Recommended message or tap its X and confirm it disappears immediately without refresh.
10. Search for a title outside the selected library cache and confirm global BookOrbit results appear as list rows. Open a result's three-dot menu and repeat with a long-press; confirm both paths show Mark as read and Mark as unread and that each action reaches BookOrbit.
11. Confirm real book covers load on Home, search results, library lists, and detail screens. Open the intended regression title `your name.` (or another book with absent/stale explicit cover metadata) and confirm foreground loading falls back to `/api/v1/books/{bookId}/thumbnail`. For a title with an explicit `/cover` URL, confirm the normalized `/thumbnail` candidate is attempted first and a failed/empty response falls through to canonical. Reopen the same unchanged title and confirm the existing versioned cache is reused. Leave the app on unmetered Wi-Fi after a full catalog refresh so the thumbnail worker can advance, then enable airplane mode and jump to several distant letters; already warmed thumbnails should render without a server response. Confirm this foreground fallback does not trigger a library-wide request-per-missing-cover scan.
12. While covers are filling in, rapidly scroll vertically and across several shelves, then stop on a distant row. Confirm off-screen requests stop competing with the visible row, its covers fill promptly, gestures remain smooth, failed thumbnails retry, and memory use does not progressively degrade. Open More and confirm About remains above the Android navigation controls.
13. Open Series and confirm there is no Load more action after loading completes. With Name/Ascending, confirm the eligible rail uses full #/A–Z order and each represented label lands on its exact initial; repeat with Name/Descending and confirm Z–A/# order. Missing labels must be disabled/grey, announced as unavailable, expose no click, and never forward to another initial. While the rail is visible, confirm the trailing card column ends before its left edge with an 8 dp visual separation at every responsive width/orientation. Select Book count, Last added, or Read progress and confirm the letter rail is hidden and the grid returns to normal 16 dp end padding. Tap Filter and verify author/library/completion/sort/direction choices rebuild the complete server result. Confirm Series cards backed by `coverBookIds` show a representative book thumbnail instead of a first-letter placeholder; a series with no covered books should retain the placeholder without requesting a nonexistent Series route. Open Authors and wait for the first screen of cards; confirm covers appear, then scroll away and back and confirm cached catalog images return quickly.
14. Open a series card and confirm it loads the complete ordered series, including series over 100 books, name/authors, read/total count, completion bar, any reported gaps, first-book synopsis, genres/tags, and book list instead of opening a reader.
15. Open a book and confirm title/subtitle, author/narrator, synopsis, publisher, publication date, language, pages, ISBN, rating, library, format, and available file metadata match the main BookOrbit detail page. Genre and Tag chips should wrap separately instead of sharing one horizontal strip; lower metadata should appear in compact Publication, Identifiers, and Library/file cards, with absent groups or fields omitted cleanly. Return and reopen the book to confirm the rich detail is immediate. Change that title's metadata in BookOrbit, refresh Lagrange, and confirm the next open replaces the old cached detail.
16. After the new detail-row work is implemented, confirm every supported width uses exactly one non-wrapping, non-scrolling action row with no clipped or off-screen actions. Read and Preview must remain labeled and inline. When no local copy exists, eligible Download must be icon-only, inline, and never relegated to More; after a local copy exists it must be absent. Delete local must be available through More and never inline. Mark as read/unread may remain inline only when it fits; otherwise it must appear in More. More must appear whenever any applicable action is hidden, preserving at least Read, Preview, eligible Download, and required More. Exercise the implemented Download/Update/Cancel/failure/retry/completion/authentication-interruption mapping and verify it preserves progress/status guidance without breaking the row. Delete the local copy through More and confirm the still-open detail returns immediately to eligible Download without backing out. After relogin, Preview a nonlocal EPUB and PDF through authenticated temporary copies and confirm Preview leaves normal progress unchanged.
   After the user-approved progress placement is implemented, verify currently-reading and read/completed details show the known canonical 0-100 percentage. Test 0%, partial, and completed values plus genuinely unknown server/local progress; unknown must not display a fabricated percentage.
17. Tap the book-detail cover and confirm it opens in a full-screen dark viewer. Tap the cover, then reopen it and tap an empty area of the screen; both taps must dismiss it. Android Back must also dismiss it. For a series book, confirm the series name and index remain visible on separate rows, tap the series name, and confirm the existing Series detail page opens. Basic Previous/Next navigation is validated. Confirm its two transparent borderless 46 dp controls match detail actions and repeated adjacent taps reuse the first complete series load without another loading delay. Recheck the new visuals with long titles, responsive widths, loading, and offline cached neighbors.
18. In a library grid, long-press or select multiple books, confirm the header does not overlap and stale selections are pruned, then apply bulk Mark as read and Mark as unread. Tap a Genre chip in Books and Series and confirm fully paginated filtered results scoped to the selected library or Series catalog; tags remain informational. For Books, the request must use singular `genre`, operator `includesAny`, and an array value; the adjacent author relation filter uses the same shape with singular `author`. Record any server-side query incompatibility or unexpected result scope.
   Completed follow-up: BookOrbit has no verified Tag filter, so Tag chips are informational and non-clickable; the instrumentation regression asserts that no click action is exposed.
19. Confirm only Home shows the Lagrange logo in the browser top bar; Library shows the selected library name selector, and other browser destinations show their page title.
20. Confirm the in-content content areas do not repeat Home/library, Series, Authors, or Local books titles above their actual cards; destination bars remain understandable.
21. Confirm the reduced typography is more compact without clipping, and book cards show one title row, an optional series row, and an index row; books without a series may use up to three title rows.
22. Confirm the Android status bar remains visible and Home content has comfortable top spacing below it.
23. Refresh the browser and confirm loading, empty, offline, and error states behave sensibly.

### 3. Reading And Listening

EPUB regression pass within version `0.2.3`: the migrated normal-Read path and restored controls have a successful Samsung Galaxy S24 functional report. Recheck `your name.`, Overlord Vol. 1, Overlord Vol. 5, and `Zero Damage Sword Saint Volume 4` after the Readium 3.0.2 upgrade, including exact locator resume, Preview isolation, and close-time progress; retain those explicit checks instead of inferring them from the functional pass.

1. Open an EPUB and confirm the chapter content renders instead of a blank/black reading surface, with no one-line-per-page layout or permanent app toolbar. Confirm Android's native top status bar remains visible with battery and current network indicators while the bottom navigation bar stays immersive. Switch among Light, Sepia, and Dark and confirm the status background matches the reader and its icons remain legible. This is the regression check for the latest debug build.
2. Confirm the footer reports measured layout-derived current/total whole-book pages and weighted completion. During initial or changed-input measurement it must show `Book pages calculating`; counts must respond to viewport, text size, margins, theme/assets, and chapter pagination changes while the footer remains visible and unobstructed. Validate measurement timing and stability on representative devices.
3. Tap the left and right outer quarters and swipe left and right; confirm each action moves exactly one paginated screen and navigation crosses chapter boundaries.
4. Confirm a swipe does not also trigger a second tap-zone navigation.
5. Tap the center and confirm it toggles lightweight chrome rather than opening full options immediately. Verify leftmost labeled Exit/X with no visible Back action below the status bar, reading-surface/center-scrim dismissal, the approximately 75%-height right page rail, page-by-one primary arrows, separate EPUB chapter arrows, and bottom Chapters/Pages plus cog actions. The outer list must expose position selection; cog options must omit chapter/page selection while retaining themes, text size, margins, Continue reading, and Close book behavior. Confirm exposed-content and Android Back dismissal order, prompt cached-browser exit behavior, final progress sync, no-cache Loading behavior, and Preview isolation remain intact.
6. In full options, confirm Light, Sepia, and Dark themes plus independent Top, Bottom, Left, and Right percentage sliders. Verify fresh/saved defaults, legibility, independent repagination, and translated-page text. Retest the three bitmap-only SVG cover samples plus the no-SVG Zero Damage control. Normalized covers must render and stay constrained; Overlord Vol. 1 chapter 3 and other ordinary/genuine-vector images must remain unchanged and be accounted for by visible/hidden pagination.
7. Select Dark, close and reopen the EPUB, and confirm Dark remains selected while the reader returns to the exact saved page within the saved chapter. Open another EPUB and confirm it also uses Dark. Repeat after fully closing and relaunching the app, then restore the preferred theme.
8. Set the device display timeout to its shortest practical value, leave an EPUB untouched for longer than that timeout, and confirm the display and reader remain open. Leave the reader and confirm normal display timeout behavior returns. A manual power-button lock or other system interruption must still work.
9. Recheck offline images, progress sync, and last-session restore against the available EPUB sample.
10. Open representative CBZ, CBR, and CB7 books. Local/readable CBZ must open in the Readium image navigator. Connected CBR/CB7 must fetch the authenticated page list/images, build a valid cached CBZ, and reuse the cached images on reopen after checking the current page count. Confirm the black fullscreen surface, 25% edge navigation, center-tap lightweight chrome, right page rail/footer, options→chrome→exit Android Back behavior, leftmost labeled Exit/X with no visible Back action, surface/scrim dismissal, exact normal-Read locator resume/progress, Preview starting at page 1 without persistence, orientation lock, keep-awake, and dark system bars. Download RAR-backed CBR and 7z-backed CB7 samples, go offline, and confirm the valid downloads are retained but show a clear server requirement; reconnect and confirm preparation/open succeeds. Client-side offline RAR/7z extraction is not implemented. Samsung Galaxy S24 validation remains required.

### 4. Downloads And Offline Reopen

1. EPUB download and local reopen are the currently validated paths.
2. Confirm the browser shows the downloaded state correctly.
3. Download a book, then make its server catalog `updatedAt` newer and refresh Lagrange. Online book details must show Update local; offline snapshots must not. Confirm Always/Never/Ask cellular behavior and update-specific progress/cancel/retry wording. Cancel once, then simulate a network failure and a malformed response: the old local copy must still open and no `.part` file may remain. Retry with valid EPUB, PDF, or comic content and confirm replacement succeeds, the action clears once the source version matches, and the new copy opens. For a legacy versionless download, confirm Update local appears once to establish a baseline. Include RAR/7z comic and nonempty MOBI/AZW3 compatibility when samples are available.
   Latest target-device result: the Update local flow works correctly.
4. Download CBZ, CBR, and CB7 samples. Confirm ZIP/CBZ reopens offline, and confirm valid RAR4/RAR5/7z downloads are retained rather than pruned as corrupt. CBR/CB7 should open through server page extraction while connected and present a clear connection requirement offline; client-side offline RAR/7z extraction is not implemented. Use the local M4B/chapter-metadata fixture for audiobook playback, download, resume, chapter, and background-service tests.

### 5. Offline Recovery

1. Put the device or emulator offline.
2. Relaunch the app and confirm the complete cached browser state is shown before network recovery finishes; Browse must not fall back to only its former first page.
3. Reopen downloaded content and verify local playback or reading still works.
4. Confirm non-downloaded titles show offline limitations cleanly.

### 6. Progress Sync

1. Read or listen long enough to create progress updates.
2. Go offline and continue generating progress.
3. Reconnect the device.
4. Close the reader immediately after a page change and confirm the final progress still leaves the debug queue and reaches the server.
5. Fully relaunch the app and confirm the first Home render already shows the current-reading shelf without opening and closing the book first.
6. Change Top and Bottom padding to visibly different values, close and reopen the same EPUB, and confirm both values persist independently and the text inset remains changed.
7. Read an unfinished EPUB in Lagrange, close the reader, and confirm the debug pending count drains only after sync. Refresh BookOrbit's web app and confirm the same title, `Reading` status, and approximate percentage appear in BookOrbit's Currently Reading view.
8. Advance that title in BookOrbit's web reader, return to Lagrange, refresh Home, and confirm the newer server percentage replaces the prior local overlay.
9. Move backward by at least 10 percentage points in Lagrange and close the reader; confirm BookOrbit accepts the lower percentage as reread/correction progress instead of retaining an old 100% or higher marker.
10. Repeat with a title below 1% progress and confirm neither side turns that value into 50% or 100%.
11. Turn several pages rapidly while a sync is likely active, immediately close the reader, and confirm the last page—not an earlier in-flight page—reaches BookOrbit and the title remains in Currently Reading.
12. Finish a title at 99.5% or above and confirm BookOrbit changes its status to `Read` and removes it from Currently Reading.
13. On a Currently Reading card, open the visible overflow menu and confirm `Remove from Currently reading` is available; repeat by long-pressing the card.
14. Using a normal account without metadata-edit permission, remove the title and confirm it disappears from Currently Reading in both Lagrange and BookOrbit and its primary/current progress resets. Pull down to refresh Home and confirm the removed title stays absent, then reopen it and confirm it starts from the beginning. Relaunch Lagrange once more to confirm stale queued or exact EPUB resume state does not restore the old position. For an audiobook, confirm BookOrbit shows zero progress. Historical reading sessions and progress on additional non-primary files are outside this fallback.
15. On Home and an individual Library Browse book, open the three-dot menu and repeat with a long-press. Confirm both paths show `Mark as read` and `Mark as unread`; collapsed series representatives should continue opening the series without single-book status actions.
16. Mark an in-progress book read and confirm it leaves Currently Reading, appears read in Lagrange and BookOrbit, and retains its prior reading position. Then mark it unread and confirm both sides report unread, progress resets, and pull-to-refresh does not restore the prior read/progress state.
17. For a series with volumes 1 and 2 complete, confirm On Deck shows volume 3. Begin volume 3 and confirm On Deck shows no later volume while volume 3 remains in Currently Reading.
18. Queue non-audio EPUB progress against a stale/deleted file ID while the book still has a different current primary file. On reconnect, confirm the first progress POST receives 404, the app fetches the current book, retries progress once with the replacement primary file, patches status, and drains the debug queue to zero. Repeat with a missing book/current file, unchanged stale ID, and replacement progress 404; each must become terminal INVALID and leave the queue instead of retrying forever. Confirm auth and transient failures still remain queued for normal recovery.
